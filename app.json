{
  "env": {
    "DATABASE_URL": {
      "description": "Database to be used"
    },
    "DJANGO_SECRET_KEY": {
      "description": "A secret key for verifying the integrity of signed cookies.",
      "generator": "secret"
    },
    "DJANGO_SETTINGS_MODULE": {
      "description": "The settings to be used, i.e. development, production etc.",
      "value": "backend.settings.production"
    },
    "DJANGO_ALLOWED_HOSTS": {
      "description": "Domain name of host the webserver runs on"
    }
  },
  "addons": ["heroku-postgresql"],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    },
    {
      "url": "heroku/python"
    }
  ]
}
